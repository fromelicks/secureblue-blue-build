# Drop-in for gnome-shell to enable mutter/KMS debug logging on demand.
# Shipped empty by default.
#
# To enable verbose mutter/KMS/DRM logging:
#   just enable-mutter-debug
#
# To disable:
#   just disable-mutter-debug
#
# Logs appear in:  journalctl --user -u "org.gnome.Shell@user.service"

[Service]
# (no env overrides here — use just commands to add a user-local drop-in)
