[Sleep]
# Use suspend-to-idle (S2Idle) instead of S3.
# Writing "freeze" to /sys/power/state triggers S2Idle directly, bypassing
# the mem_sleep selection entirely. Avoids the NVIDIA DRM re-initialization
# race (EPERM on drmModeAtomicCommit) that occurs on S3 resume.
SuspendState=freeze
