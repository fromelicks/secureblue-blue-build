[Manager]
# Reduce from the default 10min so a completely frozen system reboots sooner.
# The gnome-hang watchdog in 90-gnome-diagnostics will attempt a soft recovery
# (SIGTERM to gnome-shell) at 90s, well before this hard reboot fires.
RebootWatchdogSec=5min
