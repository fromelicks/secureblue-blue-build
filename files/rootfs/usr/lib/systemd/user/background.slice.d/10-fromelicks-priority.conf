# Background user tasks yield to both the session and foreground applications.
# Upstream default is CPUWeight=30; lower it in proportion to the app.slice and
# session.slice weights set alongside this drop-in.

[Slice]
CPUWeight=10
IOWeight=20
