#!/bin/bash
set -euo pipefail

capture_dir=/var/lib/bluetooth-captures
capture_timestamp="$(date --utc +%Y%m%dT%H%M%SZ)"

exec /usr/bin/btmon \
  --index 0 \
  --write "${capture_dir}/btmon-${capture_timestamp}.btsnoop"
